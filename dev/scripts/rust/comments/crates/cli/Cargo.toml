[package]
name = "virganol-rust-comment-checker-cli"
version.workspace = true
description = "CLI adapter for the Virganol Rust comment checker."
edition = "2021"
publish = false

[dependencies]
virganol-rust-comment-checker-core = { path = "../core" }
